# 白名单配置(可以不需要认证的请求)
spring:
  whitelist:
    paths:
      # CLUB (社团)
      - GET:/api/club/**
      # POST (帖子)
      - GET:/api/post/**
      # USER (用户)
      - GET:/api/user/**
      # SEARCH (搜索)
      - GET:/api/search/**
      # AUTH (认证)
      - POST:/api/auth/login
      - POST:/api/auth/register/**
      - POST:/api/auth/logout
      - GET:/api/auth/refresh
      # COMMENT (评论)
      - GET:/api/comment/**
      # CHAT (WebSocket握手)
      - GET:/socket.io/**
      - POST:/socket.io/**
